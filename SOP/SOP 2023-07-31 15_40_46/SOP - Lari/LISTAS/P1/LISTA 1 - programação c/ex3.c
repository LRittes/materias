/* Escreva um programa C que leia uma sequência de números inteiros da entrada padrão e calcule a
média (µ) e o desvio padrão (s) desses números, conforme as expressões abaixo (onde xi representa
cada número lido e N a quantidade de números lidos): 
A leitura deve prosseguir até que o número lido seja -1 (que não deve ser considerado no cálculo). 
O seu programa não deve assumir nenhum limite máximo de números a serem lidos, e não deve fazer uso 
de realloc(). */
