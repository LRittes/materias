/* O que acontece caso um processo tente usar uma região de memória compartilhada maior do
que a alocada? Por exemplo, caso seja alocado espaço para um vetor de 1000 inteiros, o que
acontece se um processo tentar usar 4000 inteiros? */
