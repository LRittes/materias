/* Escreva um programa C que leia da entrada padrão uma série de pares (nome prioridade), onde nome
é uma string e prioridade um valor numérico inteiro maior que zero. O programa deve manter todos os 
nomes com a mesma prioridade em uma fila, sendo que o número máximo de prioridades distintas não é 
conhecido de antemão. Quando nome for IGUAL, o programa deve imprimir todos os nomes com prioridade 
igual a prioridade, na ordem em que apareceram na entrada. Quando nome for MENIG, o programa deve 
imprimir todos os nomes com prioridades inferiores ou iguais a prioridade; os nomes devem ser 
listados em ordem de prioridade e, dentro de cada prioridade, em ordem de chegada. Quando nome for 
FIM, a execução deve ser encerrada. Na solução, o tempo gasto para listar um conjunto de nomes deve 
ser proporcional a quantidade de nomes com as prioridades em questão. Isso significa que soluções 
baseadas em uma única fila contendo todas as prioridades não são aceitáveis. */
